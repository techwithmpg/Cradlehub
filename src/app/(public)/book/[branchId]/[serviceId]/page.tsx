export default function PublicRedesignPlaceholderPage() {
  return (
    <main className="min-h-screen bg-background text-foreground">
      <div className="mx-auto flex min-h-screen max-w-4xl items-center justify-center px-6 text-center">
        <div>
          <p className="text-sm uppercase tracking-[0.3em] text-muted-foreground">
            Cradlehub
          </p>
          <h1 className="mt-4 text-4xl font-semibold tracking-tight">
            Public experience redesign in progress
          </h1>
          <p className="mt-4 text-muted-foreground">
            The old public pages have been cleared so the new premium public experience can be built cleanly.
          </p>
        </div>
      </div>
    </main>
  );
}
